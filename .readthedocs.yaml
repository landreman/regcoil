# .readthedocs.yaml
# Read the Docs configuration file, see:
# https://docs.readthedocs.io/en/stable/config-file/v2.html
#
# ADR-030: RTD compiles the Fortran extension (gfortran + BLAS) so that
# executed doc pages (MyST-NB) run the real regcoil code path, not a mock.

version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  apt_packages:
    - gfortran
    - libblas-dev
    - pkg-config
  jobs:
    pre_build:
      # Fail fast (and legibly) if the compiled extension didn't build,
      # rather than during the first executed notebook page.
      - python -c "import regcoil._core; print('regcoil._core OK')"

sphinx:
  configuration: docs/conf.py
  fail_on_warning: true

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs
